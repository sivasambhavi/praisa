-- PRAISA SQLite Database Schema
-- Mid Engineer: Implement using Antigravity prompt from docs/MID_ENGINEER_DEMO_PRD.md

-- TODO: Create patients table with all fields
-- TODO: Create visits table with foreign key to patients
-- TODO: Create indexes for performance

-- Example structure (complete this):
-- CREATE TABLE patients (
--     id INTEGER PRIMARY KEY AUTOINCREMENT,
--     patient_id TEXT UNIQUE NOT NULL,
--     hospital_id TEXT NOT NULL,
--     name TEXT NOT NULL,
--     dob DATE,
--     mobile TEXT,
--     gender TEXT,
--     abha_number TEXT,
--     address TEXT,
--     state TEXT
-- );

-- CREATE INDEX idx_patient_id ON patients(patient_id);
-- CREATE INDEX idx_abha ON patients(abha_number);
